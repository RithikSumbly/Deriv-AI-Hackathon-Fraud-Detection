# Fraud Investigation Dashboard + backend (run from project root)
# Install: pip install -r requirements.txt
# Run: streamlit run frontend/app.py

# Frontend
streamlit>=1.28.0
pyvis>=0.3.2

# Data & ML (backend)
pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
lightgbm>=4.0.0
shap>=0.43.0

# LLM (explainability, next steps, reports) — OpenAI and/or Google Gemini
openai>=1.0.0
google-generativeai>=0.3.0

# Optional: Neo4j graph features
# neo4j>=5.0.0
