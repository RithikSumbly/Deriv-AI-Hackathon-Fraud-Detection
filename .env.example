# Copy to .env and fill in. Do not commit .env (it is in .gitignore).

# Google Gemini: use your key from https://aistudio.google.com/app/apikey
# (Works as GOOGLE_API_KEY or as OPENAI_API_KEY if the key starts with AIza...)
GOOGLE_API_KEY=your_api_key_here

# Or OpenAI: set OPENAI_API_KEY=sk-...
# OPENAI_API_KEY=your_openai_key

# Optional (Google): model (default gemini-2.5-flash)
# GOOGLE_MODEL=gemini-2.5-flash

# Optional (OpenAI): base URL and model
# OPENAI_BASE_URL=https://your-endpoint.com/v1
# OPENAI_MODEL=gpt-4o-mini

# Optional: audit trail for investigator decisions (feedback store)
# INVESTIGATOR_ID=A123
# FRAUD_MODEL_VERSION=v0.3
